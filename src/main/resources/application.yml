spring:
  application:
    name: soccer-management-system
  profiles:
    default: local
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

# Removed external API tokens to avoid requiring them for local development
# football-data:
#   base-url: https://api.football-data.org/v4
#   api-token: <removed>

# api-football:
#   base-url: https://v3.football.api-sports.io
#   api-key: <removed>

springdoc:
  swagger-ui:
    path: /swagger-ui.html
